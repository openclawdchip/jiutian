# Mult Lane

## 角色

这一层负责认证链路里的线性混合。它把多个 nibble 列按固定重排和 XOR 规则交织，扩大局部变化对整组数据的影响。

## 输入

- `value`：认证混合输入。

## 输出

- `mixed`：混合后的 64 位结果。

## 处理流程

1. 读取 4x4 nibble 布局。
2. 按列抽取相关 nibble。
3. 对每列执行固定位重排和 XOR 组合。
4. 写回到对应 nibble 位置。

## 边界

- 该层是纯组合逻辑。
- 不引入状态寄存器。

## 行为模型接口

- `auth_mult`

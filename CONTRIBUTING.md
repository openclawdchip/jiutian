# 为九天 APU 贡献

九天目前处于架构阶段。最有价值的贡献应当清晰、可验证，并且对应一个
具体的设计问题。

## 贡献方向

- 架构规格与术语定义。
- APU-IR 语义。
- Agent 核 ISA 与执行模型。
- Scratchpad Memory 与 DMA 编程模型。
- NoC 拓扑、路由与 QoS。
- 面向生成代码的安全模型。
- 模拟器原型与 benchmark 框架。
- v0.1 规格稳定后的 RTL 模块。

## 基本原则

- 用可测量的技术主张替代宽泛宣传。
- 区分假设、推导与已验证结果。
- 明确区分兼容执行域与 Agent 原生执行域。
- 涉及性能主张时同时给出 benchmark 方法。
- 除非明确说明收益与代价，否则不要把全局缓存一致 SMP 语义引入
  Agent 执行面。

## Pull Request

提交 Pull Request 前，请说明：

- 要解决的问题。
- 设计选择或实现变更。
- 考虑过的替代方案。
- 测试、模拟或验证步骤。
- 若变更影响 v0.1 发布面，说明是否已检查 `docs/release-notes.md` 中的发布清单。

小而聚焦的 Pull Request 通常比大规模重写更容易评审。

## v0.1 发布前自查

涉及文档、规格、模拟器、benchmark 或测试组织的变更，应在合入前完成以下自查：

- README 与文档地图仍能引导到新增或修改后的入口。
- 规格语义变化已同步到实现覆盖矩阵。
- 模拟器行为变化已补充示例、测试或 trace 说明。
- benchmark 变化已说明 workload、baseline、指标和运行命令。
- 本地测试命令至少包含 `python -m unittest discover simulator`；若未运行，需在 PR 中说明原因。
- 已按发布负责人提供的私有禁用词表完成敏感字样检查。

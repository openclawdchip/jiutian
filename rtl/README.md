# RTL

当 v0.1 架构稳定到足以实现时，本目录将存放可综合硬件模块。

计划模块：

- Agent 核前端。
- Agent 核执行流水线。
- Scratchpad Memory。
- DMA 引擎。
- Barrier 单元。
- Cluster SRAM 接口。
- NoC packet 接口。
- Capability 与边界检查单元。

在此之前，架构行为应优先在 `simulator/` 中原型化。
